jwt.secret=${UNICOS-JWT-SECRET}
jwt.issuer=${UNICOS-JWT-ISSUER}
jwt.tempo.exp.token=${UNICOS-TEMPO-EXP-TOKEN}
jwt.tempo.exp.refresh.token=${UNICOS-TEMPO-EXP-REFRESH-TOKEN}

# Banco
spring.datasource.url=jdbc:mysql://localhost:3306/unicos_ms_autenticacao?createDatabaseIfNotExist=true
spring.datasource.username=root
spring.datasource.password=root
spring.jpa.hibernate.ddl-auto=none
logging.level.org.springframework.security=TRACE

# =========================================
# RESILIENCE4J - CONFIGURAÇÃO
# =========================================
resilience4j.circuitbreaker.instances.autenticacao-login.baseConfig=default
